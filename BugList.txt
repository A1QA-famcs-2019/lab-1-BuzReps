1) Метод getColor() возвращает цвет, не совпадающий с цветом, указанным в конструкторе
2) Конструктор не вызывает исключение при передаче null ссылки в качестве цвета
3) Конструктор не вызывает исключение при передаче пустой строки в качестве цвета
4) Конструктор не вызывает исключение при передаче отрицательного значения в качестве объема чернил
5) Конструктор не вызывает исключение при передаче отрицательного значения в качестве размера букв, которые пишутся ручкой
6) Конструктор не вызывает исключение при передаче нулевого значения в качестве размера букв, которые пишутся ручкой
7) Метод write(String word) допускает ошибки при вычислении остатка объема чернил при дробных размерах букв
8) Метод write(String word) не вызывает исключение при передаче null в качестве строки
9) Метод write(String word) неверно вычисляет длинну записываемой подстроки что приводит к вызову исключения StringIndexOutOfBoundsException
